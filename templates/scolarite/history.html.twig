{% extends('ComponentDashbord/index.html.twig') %}

{% block content %}

    <div class="row wrapper border-bottom white-bg page-heading">
        <div class="col-lg-10">
            <h2>Historique de paiement de cours de soutien de l'élève {{ nameuser.firstname }} {{ nameuser.lastname }}</h2>

        </div>
        <div class="col-lg-2">

        </div>
    </div>
    <div class="wrapper wrapper-content animated fadeInRight">
        <div class="row">
            <div class="col-lg-12">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <div class="ibox-tools">
                            <a class="collapse-link">
                                <i class="fa fa-chevron-up"></i>
                            </a>
                        </div>
                    </div>
                    <div class="ibox-content">

                        <div class="table-responsive">
                            <table class="table table-striped table-bordered table-hover " id="editable">
                                <thead>
                                <tr>
                                    <th class="text-center">Date de motification</th>
                                    <th class="text-center">Mois de paiement</th>
                                    <th class="text-center">somme</th>




                                </tr>
                                </thead>
                                <tbody>
                                {% for historie in histories %}
                                    <tr class="gradeX">
                                        <td class="text-center"> {{ historie.createdAt|date('d-m-Y H:i:s') }}</td>
                                        <td class="text-center">{{ historie.mois }}</td>
                                        <td class="text-center">{{ historie.somme }}cfa</td>


                                    </tr>

                                {% endfor %}

                                </tfoot>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
