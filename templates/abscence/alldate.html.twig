{% extends('ComponentDashbord/index.html.twig') %}

{% block content %}

    <div class="row wrapper border-bottom white-bg page-heading">
        <div class="col-lg-10">
            <h2>Liste des abscences par jours</h2>
        </div>
        <div class="col-lg-2">

        </div>
    </div>
    <div class="wrapper wrapper-content animated fadeInRight">
        <div class="row">
            <div class="col-lg-12">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <div class="ibox-tools">
                            <a class="collapse-link">
                                <i class="fa fa-chevron-up"></i>
                            </a>
                        </div>
                    </div>
                    <div class="ibox-content">

                        <div class="table-responsive">
                            <table class="table table-striped table-bordered table-hover " id="editable">
                                <thead>
                                <tr>
                                    <th class="text-center">Date d'abscence</th>
                                    <th class="text-center">Opération</th>


                                </tr>
                                </thead>
                                <tbody>
                                {% for abscencedate in abscencedates %}

                                        <tr class="gradeX">

                                            <td class="text-center"><a href="{{ path('abscenceuser', { 'idperiode': idperiode , 'idclasse':idclasse, 'createdAt':  abscencedate.createdAt|date('Y-m-d')}) }}"> {{ abscencedate.createdAt|date('d-m-Y H:i:s') }}</a>   </td>
                                            <td class="text-center">
                                                <a target="_blank" class=" btn btn-space btn-primary btn-xs sr-icons "
                                                   style="color:white;"
                                                   href="{{ path('pdfabscence', { 'idperiode': idperiode , 'idclasse':idclasse, 'createdAt':  abscencedate.createdAt|date('Y-m-d')}) }}"><i class="fa fa-1x fa-cloud-download sr-icons"></i> </a>


                                            </td>


                                        </tr>

               {% endfor %}

                                </tfoot>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
