{% extends('ComponentDashbord/index.html.twig') %}

{% block content %}

    <div class="row wrapper border-bottom white-bg page-heading">
        <div class="col-lg-10">
            <h2>Liste des abscences des éléves de la classe de <strong>{{ nameclasse.name }}</strong></h2>
        </div>
        <div class="col-lg-2">

        </div>
    </div>
    <div class="wrapper wrapper-content animated fadeInRight">
        <div class="row">
            <div class="col-lg-12">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <div class="ibox-tools">
                            <a class="collapse-link">
                                <i class="fa fa-chevron-up"></i>
                            </a>
                        </div>
                    </div>
                    <div class="ibox-content">

                        <div class="table-responsive">
                            <table class="table table-striped table-bordered table-hover " id="editable">
                                <thead>
                                <tr>
                                    <th class="text-center">Date d'abscence</th>
                                    <th class="text-center">Nom et prénom</th>
                                    <th class="text-center">Période</th>
                                    <th class="text-center">Matiére</th>


                                </tr>
                                </thead>
                                <tbody>
                                {% for absencestudent in absencestudents %}
                                    {% for  absent in absencestudent.student %}

                                        <tr class="gradeX">

                                            <td class="text-center">{{ absencestudent.createdAt|date('d-m-Y H:i:s') }}</td>
                                            <td class="text-center">{{ absent.firstname }} {{ absent.lastname }}</td>
                                            <td class="text-center">{{ id.name }}</td>
                                            <td class="text-center">{{ absencestudent.matierepresence }}</td>


                                        </tr>
                                    {% endfor %}

                                {% endfor %}


                                </tfoot>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
